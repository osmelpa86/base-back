CREATE TABLE "public"."base" (
    "base_id" UUID,
    "base_name" VARCHAR NOT NULL,
    "created_at" TIMESTAMP NOT NULL,
    "updated_at" TIMESTAMP NOT NULL,
    CONSTRAINT "base_id_pkey" PRIMARY KEY ("base_id_id")
);

